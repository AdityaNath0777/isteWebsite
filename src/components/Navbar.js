const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="nav-container">
                <a href="#" className="active">Home</a>
                <a href="#">About</a>
                <a href="#">Projects</a>
                <a href="#">Events</a>
                <a href="#">Contact Us</a>
                <a href="#">Alumni</a>
            </div>
        </nav>
    )
}

export default Navbar;